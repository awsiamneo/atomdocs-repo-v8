/** @type {import('next').NextConfig} */
const nextConfig = {
  // env: {
  //   NEXT_PUBLIC_APP_STATE: process.env.NODE_ENV === 'development' ? 'edit' : 'view',
  //   NEXT_PUBLIC_APP_TITLE: 'Atom Docs'
  // }
};

export default nextConfig;
